<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | {{ site.name }}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
    <div class="container">
      <a class="navbar-brand brand" href="/index.html">{{ site.name | upper | slice(0,4) }}<span>{{ site.name | slice(4) }}</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item">
            <a class="nav-link {% if active === 'home' %}active{% endif %}" href="/index.html">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if active === 'about' %}active{% endif %}" href="/about.html">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if active === 'contact' %}active{% endif %}" href="/contact.html">Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  {% block content %}{% endblock %}

  <footer class="bg-white border-top mt-5">
    <div class="container text-center py-4">
      <p class="text-muted mb-0">&copy; {{ site.year }} {{ site.name }}. All rights reserved.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script type="module" src="/main.js"></script>
</body>
</html>